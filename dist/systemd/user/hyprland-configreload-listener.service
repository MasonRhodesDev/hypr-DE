[Unit]
Description=Hyprland event listener (config reload, monitor changes)
After=graphical-session.target waybar.service
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=@LIBEXECDIR@/hyprland-configreload-listener.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
