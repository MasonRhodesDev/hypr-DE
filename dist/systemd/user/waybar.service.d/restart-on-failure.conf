# hypr-DE: restart policy only. The launch command lives in hypr-de.conf —
# an ExecStart here would silently win (this file sorts after hypr-de.conf).
[Service]
Restart=on-failure
RestartSec=2
StartLimitInterval=30
StartLimitBurst=5
