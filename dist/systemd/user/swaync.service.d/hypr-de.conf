# hypr-DE: launch swaync against the packaged config and the lmtt-rendered
# stylesheet.
[Service]
ExecStart=
ExecStart=/usr/bin/swaync --config @DATADIR@/swaync/config.json --style %h/.config/hypr-de/swaync.css
